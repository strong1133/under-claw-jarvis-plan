# 핵심 패턴

> Status: active · Last verified: {{YYYY-MM-DD}} · commit {{sha}} · anchor: `{{자동검증 룰 파일 경로}}`

> 이 시스템의 핵심 패턴 — 대부분 `{{룰 파일}}` 이 빌드 타임에 강제한다 (정확한 룰 수는 아래 카탈로그 참조).

## 패턴 개요

| 패턴 | 한 줄 정의 | 강제 룰 |
|---|---|---|
| ① {{패턴명}} | {{정의}} | `{{룰이름}}` |
| ② {{패턴명}} | {{정의}} | `{{룰이름}}` |
| ③ {{패턴명}} | {{정의}} | (관행 — 자동 검증 없음) |

## ① {{패턴명}}

### 의도

{{왜 이 규칙이 있는가 — 이걸 어겼을 때 실제로 무엇이 깨졌는가}}

### 올바른 형태

```{{lang}}
{{코드}}
```

### 절대 금지 (위반 → 빌드 실패)

```{{lang}}
{{안티패턴 코드}}   // ❌ {{어떤 룰 위반인지}}
```

## 룰 카탈로그

| 룰 이름 | 검사 내용 | 어길 때 증상 |
|---|---|---|
| `{{룰이름}}` | {{내용}} | {{빌드 실패 메시지}} |

<!-- 카탈로그는 룰 파일과 함께 갱신한다. 룰을 추가/삭제하고 이 표를 안 고치면
     다음 사람이 문서를 믿지 않게 된다. -->
