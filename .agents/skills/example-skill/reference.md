# {{참고 자료 제목}}

<!-- 스킬에 딸린 참고 파일. SKILL.md 가 길어질 때 분리한다 (progressive disclosure —
     에이전트는 SKILL.md 를 먼저 읽고 필요할 때만 이 파일을 연다).
     규칙 설명은 여기 쓰지 않는다. 규칙은 docs/ 가 정본이고, 여기는 복사해 쓸 형태만 둔다. -->

> 이 저장소의 실제 코드({{참고한 실제 경로}})에서 뽑은 형태다.

## {{역할 1}}

```{{lang}}
{{복사해서 시작할 수 있는 최소 골격}}
```

## {{역할 2}}

```{{lang}}
{{최소 골격}}
```

## 변형이 필요한 경우

- {{조건}} → {{다른 형태}}
