---
name: example-skill
description: {{이 스킬이 하는 일 한 문장}}. {{언제 쓰는지 — 사용자가 실제로 말할 법한 트리거 표현을 3개 이상 넣는다}}. 이 description 이 스킬 발동을 결정하므로 추상적으로 쓰지 않는다.
---

# {{스킬 이름}}

<!-- Claude Code(.claude/skills/) 와 Codex(.agents/skills/) 가 같은 포맷을 쓴다.
     두 경로의 내용은 동일해야 하며 tools/verify-instructions.sh 가 동기화를 검사한다.
     frontmatter 는 name + description 두 필드가 필수. -->

## 0. 먼저 읽기

- `docs/{{권위문서}}.md` — {{무엇의 SSOT 인지}}

이 스킬은 **절차만** 안내한다. 규칙의 정본은 위 문서이며, 충돌하면 문서를 따른다.

## 1. 판정

| 판정 | 기준 |
|---|---|
| {{선택 A vs B}} | {{판정 기준}} |
| {{만들 것인가 말 것인가}} | {{수요가 있을 때만 — 없으면 만들지 않는다}} |

## 2. 작성 순서

1. {{1단계}}
2. {{2단계}}
3. {{3단계}}

역할별 최소 골격은 [`reference.md`](reference.md) 참조.

## 3. 자주 걸리는 위반

- `{{자동검증_룰_이름}}` — {{현상}}. {{조치}}
- `{{자동검증_룰_이름}}` — {{현상}}. {{조치}}

## 4. 완료 전 확인

```bash
{{BUILD_CMD}}
{{ARCH_TEST_CMD}}
```
