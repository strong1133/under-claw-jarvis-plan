## 무엇을 / 왜

{{변경 내용과 이유. "무엇"보다 "왜"가 중요하다 — diff 는 무엇을 이미 말해준다}}

## 검증

<!-- 실제로 돌린 명령과 결과만. 안 돌렸으면 "미실행" 이라고 쓴다. -->

- [ ] `{{BUILD_CMD}}`
- [ ] `{{TEST_CMD}}` (또는 변경 범위 대응 테스트: `{{...}}`)
- [ ] `{{ARCH_TEST_CMD}}`

## 지침·문서를 건드렸다면

<!-- AGENTS.md / CLAUDE.md / .claude/** / .codex/** / docs/** 를 수정한 경우에만 -->

- [ ] `./tools/verify-instructions.sh` 통과
- [ ] 스킬을 고쳤으면 `./tools/sync-skills.sh` 로 Codex 미러 갱신
- [ ] 상세 규칙은 `docs/` 를 먼저 고쳤다 (루트/rule 은 라우팅만)
- [ ] 새 상시 규칙이라면: 실제 실패 사례가 있고, 기계로 강제할 수 없으며, 가장 좁은 scope 를 골랐다
- [ ] 개인 사정·로컬 환경을 공용 지침에 넣지 않았다 (→ `CLAUDE.local.md`)
- [ ] 고정 수치를 넣지 않았다

## 확인 필요 / 남은 것

{{판단을 유보한 지점, 후속 작업, 알려진 한계. 없으면 "없음"}}
