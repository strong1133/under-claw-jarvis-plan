---
paths:
  - "{{src/main/**/특정영역/**/*.{ext}}}"
  - "{{src/test/**/특정영역/**/*.{ext}}}"
---

# {{영역}} 규칙 라우터

<!-- rule 은 상세 규칙의 SSOT 가 아니다. "무엇을 먼저 읽어라" + "치명적 불변식 3~5개" 만 둔다.
     paths frontmatter 가 없으면 무조건 로딩되어 상시 컨텍스트를 잡아먹는다 — 반드시 넣는다.
     코드 템플릿을 여기 복사하지 않는다. 그건 skills/ 또는 docs/ 의 일이다. -->

{{언제}} 변경할 때는 `docs/{{권위문서}}.md` 의 {{절 이름}} 을 먼저 읽는다. {{그 외 경우}}에는 루트 지침으로 충분하다.

치명적 불변식:

1. {{불변식 — 어기면 빌드가 깨지거나 사고가 나는 것}}
2. {{불변식}}
3. {{불변식}}
4. 이 규칙의 적용 범위는 {{범위}} 뿐이다 — {{제외 대상}}에는 적용하지 않는다
