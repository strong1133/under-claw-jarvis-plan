# 새 워크트리를 만들 때 메인 체크아웃에서 복사할 gitignore 대상 파일.
# 여기 없는 gitignore 파일은 새 워크트리에 존재하지 않는다.

# 개인 지침·설정 (워크트리마다 다시 쓰기 번거로운 것)
CLAUDE.local.md
.claude/settings.local.json

# 로컬 실행에 필요한 비공개 설정
# {{예: src/main/resources/application-local.yml}}
# {{예: .env.local}}
